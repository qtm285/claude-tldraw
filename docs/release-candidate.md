# Frozen release candidate

The one-week clock starts only once the candidate is integrated, deployed, and
frozen. The frozen interval is one week.

Record evidence from that interval in the gate task or report, outside the
candidate's Git history, so observing the candidate does not change it.
