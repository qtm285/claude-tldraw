# tlda Weekend Plan

## Content Chips
- [ ] Text-field restriction — content pills should only insert when dropped on text input area, not anywhere on chat
- [ ] Unique tokens — `«activity:guidance activity»` collides for same agent; needs unique suffix
- [ ] Hover content — all chips from same agent show identical preview; tied to token uniqueness
- [ ] Two-chip rendering — sometimes renders two chips instead of one
- [ ] Impossible filter indicator — contradictory filters silently show nothing

## Math Notes
- [x] Dots not expanding on click — fixed (removed tabBar/done toggle, fixed indicator, stopEventPropagation)

## UI Issues
- [ ] Toolbar click not working when HUD overlaps (stacking context problem, may need structural fix)
- [ ] Auto-scroll suppression in chat on manual scrollup

## Infrastructure
- [ ] Auto-close worktree dev servers — Vite/esbuild servers orphan silently when worktrees are done. Options: `tlda` merge command that kills server as part of merge+cleanup flow, or git hook on branch delete
- [x] Shadow repo / doc version history — per-project git repo, MCP tools (doc_version, doc_preview, doc_checkout, doc_diff)
- [ ] Diff highlights — use shadow repo diff + programmatic highlights to show word-level changes on the viewer. Hover shows old text (canvasClipPanel or similar)
- [ ] save-image command — single command that saves SVG + PDF stub + PNG from one source, so agents don't have to juggle formats
- [ ] Robust draft-mode patching — biometrika.cls (and other classes that load graphicx internally) breaks the \PassOptionsToPackage{draft} approach. Need a fallback that doesn't depend on draft mode
- [ ] Keepalive routing — implementer holds keepalive until report, then delegator
- [ ] Approval prompt surfacing — detect CC permission prompts in tmux, surface in fleet chat
- [ ] Fleet/tlda factoring — fleet becomes library repo, rendering moves to tlda shapes

## Figure Rendering
- [ ] tlda LaTeX layout fidelity — stacked figures with `\\[-6ex]` look loose in viewer (individual SVGs shown separately); should compose stacked includegraphics as a single unit to match compiled PDF layout

## Viewer Panels
- [ ] Resizable front-of-canvas panels — RefViewer, build error viewer, and theorem statement viewer should be fleet-HUD-style draggable/resizable panels instead of fixed overlays, giving readers control over their reading layout

## Collaboration Features
- [ ] Per-user layout persistence
- [ ] Device mapping (iPad = viewer, laptop = editor)
- [ ] Cursor/presence indicators
- [ ] Annotation ownership display
