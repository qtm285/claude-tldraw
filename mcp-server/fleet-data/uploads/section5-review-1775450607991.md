# Section 5 Review: Degree of Balance

New subsection order: 5.1 (plug-in), 5.2 (augmentation), 5.3 (doing better), 5.4 (inner product).

---

## 1. Cross-reference problem in Section 6

**Line 1455--1457** (in Section 6, "Other estimands"):

```latex
All of our discussion above generalizes, whether focused more on maximal imbalance
as in Sections~\ref{sec:balancing-binary}-\ref{sec:role-of-augmentation}
\citep{hirshberg2021augmented} or on estimation of the inverse propensity score
as in Section~\ref{sec:inner-product} \citep{chernozhukov2018biased}.
```

The range `sec:balancing-binary` through `sec:role-of-augmentation` (4.1 through 5.2) is described as the "maximal imbalance" discussion. But Section 5.3 (`sec:doing-better`) is now between 5.2 and 5.4, and it is squarely about maximal imbalance --- Proposition 4 bounds $\imbalance_\model(\hat\gamma)$ directly. The range should extend through `sec:doing-better`:

```
as in Sections~\ref{sec:balancing-binary}-\ref{sec:doing-better}
```

---

## 2. Missing closing parenthesis

**Line 1149** (footnote in the symmetrization paragraph):

```latex
at least $1/(4\sqrt{\log(n)}$ times as large
```

Missing closing paren. Should be `$1/(4\sqrt{\log(n)})$`.

---

## 3. Grammar

**Line 1244:**

> suppose we have a valid model $\model_n$ with many elements, all of them **are** shrinking like $\dm_n$

"all of them are shrinking" doesn't work after a comma splice from "suppose we have." Either drop "are" to make it an absolute phrase ("all of them shrinking like $\dm_n$") or restructure ("suppose we have a valid model $\model_n$ whose elements are all shrinking like $\dm_n$").

---

## 4. Narrative flow

The restructured order reads well. The key dependencies are:

- **5.3 (doing better) references 5.2 (augmentation)** at line 1340: "the equicontinuity argument in Section~\ref{sec:role-of-augmentation}." This is now a backward reference. Correct.
- **5.4 (inner product) references 5.1 and 5.2** at line 1397: "The balancing arguments in Sections~\ref{sec:plugin} and~\ref{sec:role-of-augmentation}." Backward. Correct.
- **5.4 references 5.3** at line 1399: "the RKHS results of \citet{hirshberg2019minimax} discussed in Section~\ref{sec:doing-better}." Backward. Correct.
- **5.3 opening** (line 1317): "The approaches above---plug-in comparison and augmentation---" refers to 5.1 and 5.2. Backward. Correct.
- **5.4 opening** (line 1386): "The preceding subsections analyze bias through maximal imbalance." Correct --- 5.1, 5.2, and 5.3 all do.
- **"as we discuss above"** at line 1392 (in 5.4) refers to the augmentation argument from 5.2. Backward. Correct.

No forward-reference problems within Section 5 itself. The one problem is the range reference in Section 6 (item 1 above).

---

## 5. Minor style note

**Line 1143:** "we define the *symmetrized imbalance*." ends with a period before the display equation. The style guide says to flow into displays with a verb so the equation ends the sentence. A natural fix: replace the period with a colon, or drop it and let the equation serve as the sentence's conclusion. This pattern appears to be the only instance in Section 5 where a sentence terminates with a period immediately before a display.

---

## 6. Notation consistency

No issues found. `$\dgamma$` / `$\delta\gamma$` is defined in the preamble (line 193) and used consistently. `$\dm$` is introduced where needed. All equation labels referenced within Section 5 point to equations defined earlier in the manuscript or within the section itself.

---

## Summary

Two definite bugs: the Section 6 range reference (item 1) and the missing parenthesis (item 2). One grammar fix (item 3). Two minor style observations (items 4--5). No broken cross-references within Section 5 itself --- the reordering was done cleanly.
