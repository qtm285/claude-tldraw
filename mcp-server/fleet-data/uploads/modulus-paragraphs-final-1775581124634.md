# Modulus section — final draft for review

<!-- 
Replaces from "\citet{kong2025asymptotics}" through end of the paragraph before the proposition.
Existing text before this: ω definition, bias = tangent intercept, "when approximately linear the bias is small," "bounding the deviation of ω from linearity."
Figure: scratch/bias-bound-final.{pdf,svg} — single curve (competitor bound as function of s) + n^{-1/2} reference.
-->

```latex
\citet{kong2025asymptotics} and \citet{hirshberg2026bregman} show how to bound this deviation. Write $\omega(\delta) = c\,\delta + \xi(\delta)$, where $c$ is the linear growth rate of $\omega$ and $\xi(\delta) \ge 0$ is the \emph{offset complexity}. When $\gammaipw$ is representable within the model, $\omega$ is exactly linear and $\xi = 0$: the model can bridge treated-unit predictions to full-sample means via $\gammaipw$, so two functions that are close on treated units cannot differ in their means. When $\gammaipw$ is not in the model, the bridge is imperfect and $\xi$ measures the cost. Because subtracting a linear term does not change the tangent intercept, the bias depends only on~$\xi$.

\citet{hirshberg2026bregman} bounds $\xi$ by two terms. The first is the local complexity of the model at the fixed-point radius $r$ satisfying~\eqref{eq:least-squares-fixed-point}---the same localization as in Section~\ref{sec:role-of-augmentation}, bounded by $C\,r^2$. The second reflects how well $\gammaipw$ can be approximated in the model: for any $g \in \model$ with $\norm{g}_\model \le B$ and $\norm{g - \gammaipw}_{L_2(P)} \le \varepsilon$, this approximation term is at most $C\,\sigma^2 B/n$. The bound holds for any such competitor $g$, so minimizing over the choice of~$g$ gives the tightest result. Figure~\ref{fig:bias-bound} shows this bound as a function of~$s$, optimized over~$g$, for the same sample as Figure~\ref{fig:modulus}: the bound stays below $n^{-1/2}$ across most of the range, confirming that the modulus is in the nearly-linear regime visible in Figure~\ref{fig:modulus}. For the bias to be negligible---$o_p(n^{-1/2})$---it suffices that \emph{some} function in the model approximates $\gammaipw$ at finite model norm, which is automatic for nonparametric models whenever $\gammaipw$ is square-integrable. To match the local complexity rate $C\,r^2$, the stronger condition $B \le C\,r^2 n/\sigma^2$ is needed; this is the approximation condition in Proposition~\ref{prop:bias-balance}. When both terms are $O(r^2)$, the total is $C\,r^2 = o_p(n^{-1/2})$, and the conclusion is stronger than in Section~\ref{sec:role-of-augmentation}: there, augmentation with a consistent estimator was needed to convert an $O_p(n^{-1/2})$ bound on maximal imbalance into $o_p(n^{-1/2})$. Here, the minimax balancing weights achieve it without augmentation.
```
