# Weekend Status: Promised / Done / Remaining

Based on spec files (`weekend-plan.md`, `sprint-summary-w4-w6.md`, `corrected-specs.md`, `hud-layout-spec-final.md`), git log, fleet threads, and the current state of code.

---

## 1. VOICE

### Promised
- Voice recording that works reliably: say stuff, edit, keep talking, hit Enter, clean slate
- Auto-recovery when Chrome's speech API silently dies
- No text duplication (in-flight whisper response overwriting cleared textarea)
- Enter resets recording state (no shift-twice workaround needed)
- Whisper can block — latency acceptable
- Tested with real audio playback, not mocks

### Done
- Sequential whisper loop replacing setInterval (`5c60a0c`) — on main
- Right Shift = toggle only, no double-tap send (`3fe1b0e`) — on main
- 45s proactive restart + sleep/wake detection (`f6d86fc`) — on main
- Watchdog: abort(), onsoundstart, visibilitychange (`476b259`) — on main
- Generation counter bumps on manual textarea edits (`59fbe63`) — on fleet main
- MediaRecorder + whisper.cpp rewrite (`3ce45d1`) — on fleet main
- Whisper auto-starts with `tlda server start` — done today
- Research: 3 working whisper implementations found, root causes documented

### NOT done
- **Enter does not reset recording state.** After Enter-send, recording stays active. First Right Shift stops it, second starts new. This is the shift-twice bug. NOT FIXED ON MAIN.
- **Message duplication still occurs.** In-flight whisper response returns after Enter and fills textarea with just-sent text. NOT FIXED ON MAIN.
- **Longer messages vanish.** Reported by Skip today — text disappears during longer dictation. NOT INVESTIGATED.
- **Old messages get injected.** Reported by Skip today — previous message text appears in current textarea. NOT FIXED.
- **Chat switching breaks state.** Clicking into a different agent's chat corrupts voice state. NOT INVESTIGATED.
- **3-second audio segmentation.** voice-fixer was segmenting audio into 3s blobs for whisper instead of sending full accumulated audio. Skip: "THINK I BREAK MY SPEECH INTO 3S BLOBS?" 

### Who's on it
voice-fixer is alive and was working on the Enter-reset and duplication bugs. Status unclear after Skip's latest feedback about segmentation and testing approach.

---

## 2. HUD / LAYOUT

### Promised (evolved through 3 specs)

**Original spec (corrected-specs.md):**
1. HUD camera = 1:1 with canvas (not a scale model)
2. Kill the Fleet container shape — fleet shapes are normal top-level shapes
3. Global lock/unlock toggle relative to bounding box

**Final spec (hud-layout-spec-final.md, April 5):**
1. Button-activated layout mode
2. Transient container that can be resized, scaling all contents
3. Per-shape resize/move in layout mode (shapes = rectangles with texture)
4. No persistent container, no clipping, auto-resize
5. Dragging moves shapes — no chips, no chat artifacts

### Done
- **W7 v1** (`97e70bf`): Group-move overlay — reverted (`933c349`), wrong UX
- **W7 v2** (`e75bde5`): Panel drag + resize — merged, broke Skip's layout (no viewport clamping), reverted (`cbbc15d`)
- **W7 v2 clamp fix** (`09cb82f`): In worktree `w7-hud-layout-v2` — NOT MERGED
- **Layout mode** (hud-layout agent): In worktree `hud-layout-v3` — matches the final spec (transient container, proportional resize, per-shape movement). Verified in playwright. NOT MERGED.

### NOT done
- **Nothing from the HUD spec is on main.** Main has the revert — no drag, no resize, no layout mode.
- **1:1 camera** — never implemented by any agent
- **Lock/unlock toggle** — never implemented
- Both worktree implementations need review and merge

---

## 3. CONTENT CHIPS (from weekend-plan.md)

### Promised
- Text-field restriction — pills only insert on text input area drop, not anywhere on chat
- Unique tokens — `«activity:agent»` needs unique suffix to avoid collisions
- Hover content — chips from same agent show identical preview; fix tied to token uniqueness
- Two-chip rendering bug — sometimes renders two chips instead of one
- Impossible filter indicator — contradictory filters silently show nothing
- Kill iframe auto-conversion for tlda URLs (corrected-specs.md)
- File attachments (`{{att:N}}`) as ref-chip not md-file-card

### Done
- W4 chip unification (`1302c60`) — shared-doc chips with title, icon, search — on main
- Image shared-docs render inline at 75% width (`e7a38a6`, `deacb76`) — on main
- Fleet chip-pipeline branch: 1 commit (`8bcd96b`) — NOT MERGED to fleet main

### NOT done
- **Text-field restriction** — NOT DONE
- **Unique tokens** — NOT DONE
- **Hover content fix** — NOT DONE
- **Two-chip rendering bug** — NOT DONE
- **Impossible filter indicator** — NOT DONE
- **Kill iframe auto-conversion** — NOT DONE (corrected-specs.md called this "the main gap")
- **File attachments as ref-chip** — NOT DONE
- Fleet chip-pipeline branch not merged

---

## 4. PLAYBACK FRAME

### Promised
- Tldraw shape that replays fleet event streams
- Recording picker, playback tool (p key)
- Timewarp / named cuts
- Scale-to-fit
- HUD inside frame

### Done
- PlaybackFrame shape (`a811e03`) — on main
- Recording picker + PlaybackTool (`2a709ff`) — on main
- Timewarp support (`7547367`) — on main
- Scale-to-fit (`241d33e`) — on main
- HUD populate button (`bf1638c`) — on main
- Drag to move, child clipping (`1383bb4`) — on main
- Speed graph, subtitles, layout keyframes (`c03f9d0`) — on main

### NOT done
- **balancing-act doc broken** — `playback-frame` ValidationError in Yjs room. Skip: "fucking balancing act just won't load." NOBODY FIXED IT.
- No independent QA on the full playback flow

---

## 5. TERMINAL SHAPE

### Promised
Was listed as "in progress" in sprint-summary. Skip explicitly said terminal was **low priority**: "i'd love to have that working but i don't use it yet — still easier to drop into Terminal.app"

### Done
- TerminalShape with xterm.js (`c05977a`) — on main
- Schema registered (`262d17a`) — on main

### The problem
- Skip: "Bro I didn't ask for fucking xterm.js as a fucking shape dude. Read the fucking plan because you're passing shit just because it exists"
- Built without Skip's approval, merged without proper QA
- No end-to-end test (agent actually connected)
- It's on main and shipping in the bundle

---

## 6. CLUSTER SHAPE

### Promised
Listed as "in progress" in sprint-summary.

### Done
- ClusterShape (`83494c2`) — on main
- ClusterTool (`5f1f0db`) — on main

### NOT done
- No QA verification
- Skip never mentioned or confirmed this feature works

---

## 7. QA PROCESS

### Promised (corrected-specs.md)
1. Tests on worktree AND again after merge
2. QA must use playwright — not just read agent claims
3. Log review part of verification
4. Multiple screenshots, not just one token proof
5. Skip's approval required before merge to main
6. "It passes" without evidence = automatic rejection

### Done
- qa-haiku and qa-opus agents existed
- Wiretap tool implemented for QA monitoring

### NOT done
- **QA gating not enforced.** W7 HUD merged over QA's redo request.
- **QA rubber-stamps.** qa-opus checked DOM existence ("8 resize handles found"), not behavior.
- **Skip's approval not in loop.** No feature was approved by Skip before merge.
- **Retroactive QA is the norm.** QA runs after merge, has no blocking power.
- Skip to qa-opus: "raise your fucking standards. 'i couldn't test it' is not a pass"
- Skip to qa-opus: "this is supposed to be comprehensive. it's supposed to suggest i won't be surprised by failure of planned behavior"

---

## 8. INFRASTRUCTURE / SILENT FAILURES

### Promised (weekend-plan.md + corrected-specs.md)
- Auto-close worktree dev servers (Vite/esbuild orphan silently)
- Keepalive routing — implementer holds until report, then delegator
- Approval prompt surfacing — detect CC permission prompts in tmux, surface in fleet chat
- No silent failures — Skip: "our shit cannot fail silently"

### Done
- Whisper auto-starts with `tlda server start` — done today
- Dashboard renamed to `server/`, dead files deleted — done today

### NOT done
- **No health monitoring.** tlda, whisper, fleet server all go down silently.
- **No auto-restart on crash.**
- **No `tlda doctor` for self-service diagnosis.** Port checks discussed but not implemented.
- **balancing-act broken** — Yjs ValidationError, never fixed
- **Tldraw store wipe** — Skip asked for stores to be wiped on all projects. NOT DONE.
- **Worktree cleanup** — NOT DONE
- **Keepalive routing** — NOT DONE
- **Approval prompt surfacing** — NOT DONE
- **Fleet/tlda factoring** — noted in weekend-plan, NOT STARTED
- **Compaction watcher** — missed a compaction event, NOT FIXED

---

## 9. BROWSER TOOLS

### Promised (corrected-specs.md)
- `reload_browser(reason)` — AppleScript reload, gated on chat permission
- `get_console(level?, limit?)` — inject console collector, query via AppleScript

### Done
- Both tools implemented in fleet MCP server code by guidance

### NOT done
- **Needs MCP server restart for agents to see the tools.** Not yet active.

---

## 10. FOOT CONTROL / GESTURES

### Promised
- Sounds-as-gestures research
- Tongue click classifier investigation
- Integration with existing clickDetect.ts

### Done
- T-Pedals axis fix, physics, click improvements (`ef04442`) — on main
- Doc-panel hover, debug panel position (`0cf18c4`) — on main
- Reverse gesture + editable response curve (`6e9f29d`) — on main
- Sensitivity slider to 50x (`77c9fda`) — on main
- Pillify control panel + Start Mic button (`d90c9d0`) — on main
- Disable tongue clicks outside foot mode (`91408a4`) — on main
- Gesture research report complete (whistle/hiss detection, TF.js transfer learning)

### NOT done
- **Whistle/hiss detection implementation** — research only
- **TF.js transfer learning for custom sounds** — research only

---

## 11. OTHER FROM WEEKEND PLAN

### Shadow History / Doc Versioning
- **Done:** Shadow history scrubber overlay (`0ce554e`) — on main
- **NOT done:** Diff highlights using shadow repo + programmatic highlights. Word-level change display.

### Math Notes
- **Done:** Dots click fix (checked off in weekend-plan)
- **Done:** Math-note doc bridge (`2ee9bfc`) — on main. No interactive test.

### Figure Rendering
- **NOT done:** Stacked figures with `\\[-6ex]` still show as individual SVGs

### Viewer Panels
- **NOT done:** Resizable front-of-canvas panels (RefViewer, build error viewer, theorem statement viewer)

### Collaboration
- **NOT done:** Per-user layout persistence, device mapping, cursor/presence, annotation ownership

### Other Merged
- Folder drag in chat (`799b64c`) — on main
- File upload on drop (`e8ac3f6`) — on main
- Local image ref rewriting (`38f8a2e`) — on main
- Fleet toolbar overflow tools (`edee9ec`) — on main
- Preamble macros dynamic loading (`4a76445`) — on main
- TeX diff with KaTeX (`d81a911`) — on main
- Stale agent text darkened (`ba87c7c`) — on main
- URL error UI for 404/401/403 (`d645a5c`) — on main

---

## 12. TASK INBOX

### Promised (corrected-specs.md)
- Passive review, not active approval
- Completed work auto-queues with evidence
- Playbacks auto-advance
- Reject = one tap, approve = keep scrolling
- Lives in fleet chat default view

### Done
- task-inbox agent built v1 — waiting Skip's review

### NOT done
- **Never reviewed by Skip**

---

## 13. SANDBOXED QA (DeepSeek in Docker)

### Promised
- Docker/OrbStack for sandboxing untrusted model agents
- Read-only filesystem, limited tools, fresh sessions

### Done
- Spec written (`scratch/sandboxed-qa-agent-spec.md`)

### NOT done
- **No Docker/OrbStack installed. No implementation.**

---

## Summary

| Area | Promised | Done | Remaining |
|------|----------|------|-----------|
| Voice | 6 requirements | Whisper rewrite, watchdog, auto-start | Enter-reset, duplication, vanishing text, old message injection |
| HUD Layout | Final spec: layout mode | Built in worktree (not merged) | Everything — main has revert only |
| Chips | 7 items from weekend-plan | 2 items (unification, inline images) | 5 items (text-field restrict, unique tokens, hover, two-chip bug, impossible filter) |
| Playback | Full feature set | Mostly done | balancing-act broken, no QA |
| Terminal | Low priority per Skip | Built and merged | Skip didn't ask for it |
| Cluster | In progress | Built and merged | No QA, not confirmed by Skip |
| QA Process | 6 requirements | Agents exist | Process not enforced at all |
| Infrastructure | 9 items | Whisper auto-start, dashboard cleanup | Health monitoring, auto-restart, store wipe, worktree cleanup, etc. |
| Browser Tools | 2 tools | Implemented | Needs MCP restart to activate |
| Foot Control | Gestures + research | 6 commits merged + research done | Implementation of new gestures |
| Task Inbox | Passive review | v1 built | Never reviewed |
| Sandboxed QA | Docker spec | Spec only | No implementation |
