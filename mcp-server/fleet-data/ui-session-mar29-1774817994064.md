# Fleet UI Session — March 29

## Done

### Agents Panel
- **No status dots** — removed colored circles, activity shown via name opacity (1.0 active, 0.5 stale, 0.3 dead)
- **Column reorder** — Agent, Seen, Task, Labels (matches old dashboard feel)
- **Label chip opacity** at 75%
- **Dead CSS** cleaned up (dot column styles)

### Fleet Shapes
- **No rounded corners** — borderRadius 0 on chat, agents, search, container. Snapped shapes look clean.

### Chat
- **Placeholder** — "Message..." replaced with "-> target" or empty
- **Filter overlay** — only opens on agent/label pill drag, not content drags (timestamps, code blocks)
- **Search placeholder** — standard fleet colors (var(--text-dim)), not amber

### HUD Camera Tracking
- Horizontal position follows `(fleetBounds.x + cam.x) * cam.z` via RAF polling on mainEditor camera
- Pan left/right moves the HUD, keeping fleet shapes at fixed position relative to doc margin
- Collapsed pill stays in default position, only expanded HUD tracks

### Pills Layout
- Fleet pill moved out of build-pills-row into its own flex item in bottom-panels column
- Stacks vertically above tldraw watermark, no overlap (3px gap)
- Watermark faded to 0.12 opacity to match fleet pills visual salience

### MD File Drag to Canvas
- `.md-file-card` and `.tlda-card` elements in chat are draggable as doc pills
- Drop on canvas: shared docs navigate directly, raw files go through `/api/share-file`
- Server endpoint reads file, creates tlda project, pushes content, adds to fleet-workspace book
- Uses RW token from `~/.config/tlda/config.json`

### Cleanup
- Deleted `bin/fleet-respawn` and `bin/restart-both`
- fleet-spawn task closed

## Not Done

- **Margin shape** — tabled, easy to position fleet shapes manually
- **Browse tool state machine** — needs design discussion for HTML books + fleet UI
- **Arrow refs** — `[->Theorem 3.2]` wired up by cc-fork (in same commit batch)
